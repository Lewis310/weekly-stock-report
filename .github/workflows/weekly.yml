name: Morning Market Report

on:
  schedule:
    - cron: '30 10 * * 1-5'  # 6:30 AM ET = 10:30 UTC (Monday-Friday)
  workflow_dispatch:  # Allow manual triggers

jobs:
  send-report:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install yfinance pandas matplotlib openai
        
    - name: Send market report
      env:
        EMAIL_TO: ${{ secrets.EMAIL_TO }}
        FROM_NAME: ${{ secrets.FROM_NAME }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        SMTP_USER: ${{ secrets.SMTP_USER }}
      run: |
        python weekly_stock_report.py  # ← Changed to match your actual filename
